import React from 'react';
import styled from 'styled-components';

export const LogoHeader = styled.label`
  font-size: 25px;
  line-height: 75px;
  font-weight: bold;
`;

export const LogoHeaderBig = styled.label`
  font-size: 30px;
  font-weight: bold;
`;
