import React from 'react';
import styled from 'styled-components';

const TrainerSchool = styled.p`
  margin-bottom: 0px;
  font-size: 14px;
  color: rgba(103, 107, 105, 0.5);
`;

export default TrainerSchool;
